#!/usr/bin/env python
from setuptools import setup

setup(
    name='django_jquery_validate',
    version='0.5',
    description='Jquery form validation in django',
    author='Jordi Sesmero',
    packages=['django_jquery_validate'],
    install_requires=["django>=1.3"])
